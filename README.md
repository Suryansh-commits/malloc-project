# malloc-project
A lightweight custom memory allocator in C that manually manages heap blocks using linked lists and metadata headers. It supports allocation, deallocation, splitting, and coalescing using the sbrk() system call.
